# Kamm 5/3/3 自包含管线依赖
# 环境: Python 3.10+ (已验证 3.14), NVIDIA GPU + CUDA

# 训练 + 推理核心
torch>=2.4          # PyTorch (含 CUDA 构建, 按 https://pytorch.org 安装对应版本)
numpy>=1.26         # 数值计算

# 可视化 (viz.py)
matplotlib>=3.8     # 绘图 + GIF 动画
pillow>=10.0        # PillowWriter GIF 输出
